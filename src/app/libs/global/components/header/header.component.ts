import { Component } from "@angular/core";

@Component({
    selector: 'tak-header',
    templateUrl: './header.component.html',
    styleUrls: ['./header.component.scss']
}) 

export class TakHeaderComponent {
    // TODO: Split into shared header that controls navigation
}